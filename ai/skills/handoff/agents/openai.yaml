interface:
  display_name: "Handoff Prompt Generator"
  short_description: "Draft model-specific prompts for agent handoffs"
  default_prompt: "Use $handoff to draft a lean execution-ready prompt for the named target while preserving caller-selected runtime and delivery policies. Add commit, signing, clean-worktree, self-review, or external-review requirements only when explicitly authorized."
