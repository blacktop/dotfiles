interface:
  display_name: "Handoff Prompt Generator"
  short_description: "Draft model-specific prompts for agent handoffs"
  default_prompt: "Use $handoff to draft an execution-ready prompt for the named target model while preserving caller-selected runtime settings."
